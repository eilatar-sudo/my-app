# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Key-Compromise Quarantine Plane](docs/agentic-key-compromise-quarantine-plane.md):
deterministic, tenant-scoped quarantine of receipts signed after a key
retirement watermark without rewriting attested history or inventing success.

```bash
npm install
npm run validate
```
