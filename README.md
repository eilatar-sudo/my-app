# mondayDB Agentic Strategy

Daily strategy contracts that move mondayDB from a WorkOS engine toward a deterministic Agentic Database.

This increment ships the **Promotion-Containment-Expiry Review Plane**: dual-control steward paging after `CONTAINMENT_EXPIRED` without rewriting silence as `TRUSTED_ENROLLED`.

```bash
npm install
npm run validate
```
