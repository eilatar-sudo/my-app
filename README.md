# mondayDB Agentic Strategy

Daily strategy contracts that move mondayDB from a WorkOS engine toward a deterministic Agentic Database.

This increment ships the **Promotion-Resolution-Expiry Containment Plane**: dual-control freeze after `RESOLUTION_EXPIRED` without rewriting silence as `TRUSTED_ENROLLED`.

```bash
npm install
npm run validate
```
