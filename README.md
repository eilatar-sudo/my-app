# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Envelope Tool-Effect Saga Plane](docs/agentic-envelope-tool-effect-saga-plane.md):
deterministic, tenant-scoped saga binding for gated `TOOL_MUTATE` decisions
with `UNKNOWN_EFFECT` compensation honesty.

```bash
npm install
npm run validate
```
