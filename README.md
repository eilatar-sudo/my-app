# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Provider-Key Rotation Plane](docs/agentic-provider-key-rotation-plane.md):
deterministic, tenant-scoped retirement of receipt-signing material
without rewriting attested history or inventing success.

```bash
npm install
npm run validate
```
