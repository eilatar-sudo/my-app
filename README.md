# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Envelope Purpose Gate Plane](docs/agentic-envelope-purpose-gate-plane.md):
deterministic, tenant-scoped read/tool authorization for hop-attenuated
citations placed into compiled working sets.

```bash
npm install
npm run validate
```
