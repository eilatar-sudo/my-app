# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Watermark-Clock Attestation Plane](docs/agentic-watermark-clock-attestation-plane.md):
deterministic, tenant-scoped binding of retirement and compromise watermarks
to an attested provider clock without rewriting history or inventing success.

```bash
npm install
npm run validate
```
