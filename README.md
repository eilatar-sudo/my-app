# my-app

mondayDB agentic-database strategy contracts.

The current increment is the [Provider-Receipt Attestation Plane](docs/agentic-provider-receipt-attestation-plane.md):
deterministic, tenant-scoped binding of `UNKNOWN_EFFECT` saga compensations
to trusted tool receipts without inventing success.

```bash
npm install
npm run validate
```
