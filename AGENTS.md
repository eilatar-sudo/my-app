# AGENTS.md

## Cursor Cloud specific instructions

This is a new/empty repository (`my-app`) containing only a `README.md` and an MIT `LICENSE`. There is currently no application code, build system, or services.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Makefile`, or similar exists yet.
- **No services to run** — no backend, frontend, database, or other services are configured.
- **No tests or linting** — no test framework or linter is set up.

When application code is added in the future, update this section with:
1. How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
2. How to run lint, tests, and the dev server.
3. Any non-obvious startup caveats or environment requirements.
